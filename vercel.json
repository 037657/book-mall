{
  "installCommand": "npm install --workspace=frontend",
  "buildCommand": "npm run build --workspace=frontend -- --outDir ../dist --emptyOutDir",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
